version: 1
formatters:
  json:
    format: "%(asctime)s %(levelname)s %(name)s %(message)s"
handlers:
  console:
    class: logging.StreamHandler
    formatter: json
loggers:
  app:
    level: INFO
    handlers: [console]
    propagate: False
root:
  level: INFO
  handlers: [console]